{
  "name": "bands-exchange",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "bands.finance Play: the Bands Exchange room server (Cloudflare Worker + one Durable Object)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.138.0"
  }
}
