node_modules
dist/

# secrets: never commit
.env
.env.*
!.env.example
!.env.platform.example

# runtime state + journal data
/data/
STOP

*.log
.DS_Store
web/artifact/
.vercel/
web/.vercel/
data-test/
hot.json
web/public/history.json
data-once/
data-test*/
data-paper/
data-paper-ray/
data-paper-usdc/
data-paper-stock/
data-live/
data-paper-stock/
web/dist-dash/
data-mainnet/
data-talk/

# Blender backup saves
*.blend1

# the site's book snapshots: written by `npm run web:snapshot` before every deploy (src/publish/snapshot.ts),
# never committed, so no desk's book (and never a paper one) rides into git
web/public/journal.json
web/public/equity.json
web/public/learned.json
data-video/
